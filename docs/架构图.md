# Voice-Canvas 整体架构图

> 本图用于说明 Voice-Canvas 的核心数据流，以及"框架负责手和眼，自研层负责大脑"的边界。T5 起补充后端 ASR 层，但语义编译和执行层契约不变。

```mermaid
flowchart LR
  User["用户语音指令"]

  subgraph FrameworkIO["框架 / 基础能力层：手和眼"]
    ASR["语音采集\nWeb Speech / MediaRecorder"]
    Canvas["画布渲染\nKonva + react-konva"]
    ModelSDK["模型 SDK / HTTP\nClaude + DeepSeek"]
    BaiduASR["百度 ASR REST\n短语音 <= 60s"]
  end

  subgraph Brain["自研语义大脑层"]
    ASRState["ASR 状态机\nactive / standby / off\npartial/final 去抖"]
    FastPath["本地快速通道\n暂停/继续/撤销/确认词"]
    Prompt["拆解 Prompt\n场景图 + 最近对话 + 规则"]
    DSL["指令 DSL / Zod Schema\nResponseEnvelope + Operation"]
    Layout["定位模型与确定性布局器\nabsolute / anchor / relative / layout"]
    Scene["场景图 / 状态层\nobjects / groups / focus / history"]
    Guard["执行前校验与容错\nschema 校验 / pendingAction / clarify"]
  end

  subgraph Server["薄后端代理层"]
    API["REST /api/parse"]
    ASRAPI["REST /api/asr"]
    ASRProvider["AsrProvider\nBaidu token + 格式校验"]
    Router["ModelProvider 路由\nClaude 主用 + DeepSeek 兜底"]
    Env["环境变量注入\nAPI key 不进前端"]
  end

  User --> ASR
  ASR -->|"browser final transcript"| ASRState
  ASR -->|"audio chunk <=55s"| ASRAPI
  ASRAPI --> ASRProvider
  Env --> ASRProvider
  ASRProvider --> BaiduASR
  BaiduASR -->|"transcript"| ASRState
  ASRState -->|"会话控制 / 唤醒词"| FastPath
  FastPath --> Scene
  ASRState -->|"绘图 / 编辑 final transcript"| Prompt
  Scene -->|"紧凑序列化"| Prompt
  Prompt --> API
  API --> Router
  Env --> Router
  Router --> ModelSDK
  ModelSDK -->|"严格 JSON"| DSL
  DSL --> Guard
  Guard -->|"需要澄清"| User
  Guard -->|"等待确认"| FastPath
  Guard --> Layout
  Layout --> Scene
  Scene --> Canvas

  classDef framework fill:#eef6ff,stroke:#3b82f6,color:#0f172a;
  classDef brain fill:#fff7ed,stroke:#f97316,color:#0f172a;
  classDef server fill:#f0fdf4,stroke:#22c55e,color:#0f172a;
  classDef user fill:#f8fafc,stroke:#64748b,color:#0f172a;

  class ASR,Canvas,ModelSDK,BaiduASR framework;
  class ASRState,FastPath,Prompt,DSL,Layout,Scene,Guard brain;
  class API,ASRAPI,ASRProvider,Router,Env server;
  class User user;
```

## 分层说明

| 数据流层 | 主要职责 | 边界 |
|---|---|---|
| 语音采集层 | 接收用户语音，输出 transcript | 浏览器 ASR 或后端百度 ASR 提供原始识别能力；ASR provider、状态机、60 秒上限保护由项目自研 |
| 语义编译层 | 把自然语言和场景上下文编译为结构化 JSON 操作 | LLM 负责语言理解；Prompt、DSL、Zod 校验由项目自研 |
| 场景图 / 状态层 | 保存 objects、groups、focus、history、pendingAction | 这是渲染真相与上下文记忆的核心自研层 |
| 执行渲染层 | 把操作落到画布，并按场景图渲染 | Konva 负责绘制；布局器、执行前校验、容错规则由项目自研 |
| 模型调度层 | 后端注入 key，按模型路由并自动兜底 | Express 只是薄代理；Provider 路由和 failover 是项目控制逻辑 |
| 后端 ASR 层 | 后端藏百度 key，调用短语音识别，返回 transcript | 百度只负责语音转文字；录音窗口、格式校验、失败不执行由项目控制 |
