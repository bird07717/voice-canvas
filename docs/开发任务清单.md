# Voice-Canvas 开发任务清单（两天 backlog）

> 本文档把三份设计文档（`MVP方案.md` / `大脑设计.md` v2 / `指令能力清单.md`）落地为可执行的开发步骤。
> 按设计文档 §九 两天排期组织为 5 个阶段（阶段 0 → 阶段 4），每个任务标注：目标、关键步骤、涉及文件、验收标准、对应文档锚点。
> 使用方式：按阶段顺序推进，每完成一个任务在 `状态` 列打勾，并回填 `指令能力清单.md` 的状态列；代码变更按下方 PR 规则拆分提交。
>
> 状态图例：⬜ 待开发 / 🟡 进行中 / ✅ 已完成 / ❌ 阻塞

---

## Git / PR 协作规则（强制）

1. **不在主分支开发**：主分支只接收已通过检查的 PR。任何功能、修复、文档更新都从独立分支开始，如 `feature/scene-store`、`fix/asr-echo-suppression`、`docs/task-backlog-pr-flow`。
2. **基于 PR 添加新功能**：每个开发任务完成后，推送当前功能分支并创建 PR；禁止把多个无关功能攒成一个大 PR。
3. **每个 PR 只做一件事**：一个 PR 只实现或修改单一功能。大功能拆成多个独立 PR，例如 `schema`、`scene store`、`Konva renderer`、`layout row/grid` 分开提交。
4. **PR 标题与描述必须完整**：
   - 标题：一句话说明本 PR 新增/修改了什么。
   - 功能描述：说明该功能的作用与使用方式。
   - 实现思路：简要说明技术选型或核心实现逻辑。
   - 测试方式：说明如何验证该功能正常运行。
5. **推荐 PR 模板**：

```md
## 功能描述

## 实现思路

## 测试方式
```

6. **推荐命令流程**：

```bash
git checkout main
git pull
git checkout -b feature/<single-purpose>
git add <files>
git commit -m "feat: <single-purpose>"
git push -u origin feature/<single-purpose>
# 然后创建 PR，按模板填写标题与描述
```

---

## 总览：阶段与里程碑

| 阶段 | 对应排期 | 里程碑（做完即可演示的能力） | 任务数 |
|---|---|---|---|
| **阶段 0｜工程前置** | Day1 上午前置 | 架构图 + 工程骨架可 `npm run dev` 起双进程 + README 骨架 | 5 |
| **阶段 1｜执行层闭环** | Day1 上午 | 按钮手动触发，能画/改色/移动/删除/撤销基础图形 | 6 |
| **阶段 2｜语音最小闭环** | Day1 下午 | 说"画一个红色的圆" → 真的画出来 | 7 |
| **阶段 3｜大脑深度层** | Day2 上午 | 房子拆解、五个渐变圆排一排、相对定位、上下文指代、模型兜底 | 7 |
| **阶段 4｜容错与收尾** | Day2 下午 | 危险指令二次确认、澄清对话、思考流、UI 打磨、文档、录视频 | 8 |

**关键依赖链**：阶段 0 → 阶段 1（执行层是一切的"手"）→ 阶段 2（接通语音和 LLM）→ 阶段 3（布局器/group 依赖执行层）→ 阶段 4（容错和 UI 依赖前面全部）。阶段内部任务尽量按编号顺序，标注「可并行」的可穿插做。

---

## 阶段 0 — 架构图与工程初始化（Day1 上午前置）

> 目标：把空仓库变成一个能跑起来、目录清晰、key 安全的工程骨架。这一阶段不写业务逻辑，只搭地基。

### T0.1 画整体架构图（标清自研层 vs 框架层）✅

- **目标**：一张图说清"框架负责手和眼，自研层负责大脑"，作为设计文档配图和视频素材。
- **步骤**：
  1. 用 Mermaid 画五层数据流：语音采集 → 语义编译 → 场景图/状态 → 执行渲染 → 模型调度。
  2. 明确标注哪些是框架（Web Speech / Konva / LLM SDK），哪些是自研（场景图、定位模型、DSL、拆解 prompt、布局器、ASR 状态机）。
  3. 存为 `docs/架构图.mermaid` 或 `docs/架构图.md`。
- **验收**：图中能一眼区分自研层与框架层；与 `大脑设计.md` §0 数据流一致。
- **文档锚点**：MVP方案 §二、§十；大脑设计 §0。
- **优先级**：P0（叙事价值高，先做不亏）。

### T0.2 初始化 monorepo 工程骨架 ✅

- **目标**：建立 `/client` + `/server` 双目录结构，能各自独立启动。
- **步骤**：
  1. 根目录 `npm init`，建 `/client`（Vite + React + TS）和 `/server`（Express + TS）。
  2. `/client`：`npm create vite@latest client -- --template react-ts`，装 `konva react-konva zustand zod`。
  3. `/server`：装 `express cors dotenv`，TS 配置（`tsx` 或 `ts-node-dev` 做热重载）。
  4. 根 `package.json` 装 `concurrently`，写 `npm run dev` 同时起两个进程。
  5. Vite 配 `/api` 代理到 Express 端口（如 `localhost:3001`）。
- **验收**：`npm run dev` 一条命令同时起 Vite（前端）和 Express（后端）；浏览器能打开空白 React 页，前端 `fetch('/api/health')` 能打到后端返回的探活响应。
- **文档锚点**：MVP方案 §三「工程形态」；大脑设计 §11。
- **优先级**：P0。

### T0.3 配置 key 管理与环境变量 ✅

- **目标**：真实 key 绝不进仓库，运行时注入。
- **步骤**：
  1. `/server` 建 `.env`（放真实 key）和 `.env.example`（占位）。
  2. 根 `.gitignore` 忽略 `.env`、`node_modules`、`dist`。
  3. 后端只读 `process.env.ANTHROPIC_API_KEY` / `process.env.DEEPSEEK_API_KEY`，预留 `*_BASE_URL` 适配中转。
- **验收**：`git status` 不出现 `.env`；`.env.example` 含全部需要的键名但无真值。
- **文档锚点**：MVP方案 §七。
- **优先级**：P0（安全铁律，越早越好）。

### T0.4 README 运行骨架 ✅

- **目标**：不要把 README 全堆到最后，先提供最小运行说明，后续阶段持续补充。
- **步骤**：
  1. 根目录新增/更新 `README.md`。
  2. 写清本地运行方式：安装依赖、配置 `/server/.env`、运行 `npm run dev`。
  3. 说明 `.env.example` 中每个 key 的用途，不写真实 key。
  4. 简要说明项目结构：`/client`、`/server`、`/docs`。
- **验收**：新同学 clone 后能按 README 跑起空项目；README 不承诺未实现功能。
- **优先级**：P0。

### T0.5 建立功能分支与首个 PR ✅

- **目标**：从第一步开始执行“不在主分支开发、每个 PR 只做一件事”的协作规则。
- **步骤**：
  1. 从主分支新建功能分支，如 `feature/project-skeleton`。
  2. 确认 `.gitignore` 生效后提交工程骨架与 README 骨架。
  3. `git push -u origin feature/project-skeleton`。
  4. 创建 PR，按模板填写标题、功能描述、实现思路、测试方式。
- **验收**：主分支没有直接开发提交；PR 描述完整；提交内不含任何 key 或 `node_modules`。
- **优先级**：P0。

---

## 阶段 1 — Konva 执行层闭环（Day1 上午）

> 目标：先用按钮手动触发，把"场景图 → Konva 渲染"这条确定性管线跑通。这一层是 dumb 且可靠的"手"，不涉及任何 LLM。先有手，才谈大脑。

### T1.1 定义场景图数据结构、Zod schema 与 Zustand store ✅

- **目标**：实现 `SceneObject` / `Group` / `SceneState` 与核心 Operation schema，作为渲染和执行的共同契约。
- **步骤**：
  1. 按 `大脑设计.md` §1.2/§1.3/§1.4 定义 TS 类型：`SceneObject`（id/type/geometry/style/label/groupId/seq/z）、`Group`（id/label/memberIds/bbox/seq）、`SceneState`（objects/groups/lastCreatedIds/focusIds/canvas/pendingAction/nextSeq）。
  2. 提前落地 Zod 核心 schema：`Geometry`、`Style`、`Position`（至少支持 `absolute` / `anchor`）、`Operation`（create/setStyle/transform/delete/clear/undo/redo）。
  3. 用 Zustand 建 store 持有 `SceneState`，**数据与渲染解耦**（不把场景图塞进 React state 频繁深拷贝）。
  4. 实现 bbox 推导工具（由各 type geometry 算 `{x,y,w,h}`，§1.2 约定）。
- **验收**：能在 store 里手动塞一个 circle 对象；合法 operation JSON 通过 Zod 校验，非法字段被拒。
- **文档锚点**：大脑设计 §1、§3.2、§3.3。
- **优先级**：P0。

### T1.2 Konva 画布渲染层 ✅

- **目标**：场景图变化时，Konva 画布正确渲染对应图形。
- **步骤**：
  1. `react-konva` 建全屏 `Stage` + `Layer`，画布尺寸按 `canvas.width/height`。
  2. 写 `<SceneRenderer>`：遍历 `objects`，按 type 渲染 Circle/Rect/Triangle/Line/Text/Ellipse。
  3. **统一中心点定位**：rect/triangle 的 Konva 左上角 = `x - w/2, y - h/2`，circle 直接用中心（§1.5）。
  4. 用库的局部更新/分层，避免每次全量重绘（性能注意点）。
- **验收**：store 里塞 3 个不同图形，画布正确显示，位置符合中心定位约定。
- **文档锚点**：大脑设计 §1.5；MVP方案 §三「性能注意」。
- **优先级**：P0。

### T1.3 实现 create / setStyle / transform / delete 执行器 ✅

- **目标**：实现核心 operation 的确定性 apply 逻辑（先不接 LLM，直接喂 JSON）。
- **步骤**：
  1. 实现 `applyOperation(op, state)`：`create`（新建对象，分配 seq/id）、`setStyle`（改 fill/stroke/...）、`transform`（move/moveTo/scale/resize/rotate）、`delete`。
  2. 每次执行更新 `focusIds` / `lastCreatedIds` / `nextSeq`。
  3. 颜色归一化：中文 → 十六进制色表（§3.6）。
  4. 提前实现 `absolute` / `anchor` 位置解析，保证 "画一个红色的圆" 可在 Day1 闭环里落到画布中心或指定九宫格位置。
- **验收**：在调试面板手动构造 operation JSON，能正确创建、改色、移动、删除对象；`position.mode = "anchor"` 可正确定位到画布中心/九宫格。
- **文档锚点**：大脑设计 §2.2、§3.3、§5、§3.6。
- **优先级**：P0。

### T1.4 撤销 / 重做（历史快照栈）✅

- **目标**：实现 `undo` / `redo`。
- **步骤**：
  1. 每次成功执行操作前，深拷贝 `objects + groups` 压撤销栈（§1.4）。
  2. `undo` 弹栈恢复并把当前态压重做栈；`redo` 反向。
  3. group 操作以"整组"为一次撤销步。
- **验收**：画 3 个图形后连续撤销/重做，画布状态精确回退/恢复。
- **文档锚点**：大脑设计 §1.4、§3.3。
- **优先级**：撤销 P0 / 重做 P1。

### T1.5 开发调试面板（按钮手动触发）✅

- **目标**：在没接语音前，用按钮触发上述全部操作，验证执行层。
- **步骤**：侧边栏临时放按钮："画圆""画方块""改色""左移""删除""撤销""清空"，各自构造一个 operation 数组喂执行器。
- **验收**：纯靠点按钮能完成 L1+L2+L3（撤销/清空）的基础闭环。
- **说明**：这些按钮是开发期/故障恢复入口，**不出现在正式演示路径**（MVP方案 §五）。
- **优先级**：P0（开发脚手架）。

### T1.6 执行层 PR 检查点 ✅

- **验收**：阶段 1 的变更按单功能拆成小 PR（如 `scene schema/store`、`Konva renderer`、`operation executor`、`debug panel`），每个 PR 描述含功能描述、实现思路、测试方式。阶段结束时能演示"点按钮画图改色移动删除撤销"。

---

## 阶段 2 — 语音最小闭环（Day1 下午）

> 目标：打通"说话 → 画图"。接 Web Speech API（带 ASR 状态机雏形）+ 后端 LLM 代理 + Zod 校验。做完这一阶段，"画一个红色的圆"能靠嘴完成。

### T2.1 响应包络 schema 与 LLM 返回校验 ✅

- **目标**：在阶段 1 已有 Operation schema 的基础上，补齐 LLM 响应包络校验，防止模型输出污染执行层。
- **步骤**：
  1. 实现 `ResponseEnvelope`（understanding/operations/reply/clarify）。
  2. 将 `ResponseEnvelope.operations` 接入阶段 1 的 `Operation` schema。
  3. 实现模型返回 JSON 解析失败 / schema 校验失败时的错误报告，不直接执行。
- **验收**：合法响应包络通过校验；非法 JSON、缺字段、非法 operation 都会被拒并能定位错误。
- **文档锚点**：大脑设计 §3.1–§3.3。
- **优先级**：P0。
- **可并行**：与 T2.2/T2.3 后端代理可并行开发。

### T2.2 MockProvider 与 fixtures（脱离模型的稳定开发入口）✅

- **目标**：在真实 LLM 接入前，提供固定 JSON 响应，避免 key、网络、模型格式问题阻塞前端/执行层开发。
- **步骤**：
  1. 定义 `ModelProvider.parseInstruction(input): Promise<ParseResult>` 接口。
  2. 实现 `MockProvider`，对 "画一个红色的圆"、"把它变成蓝色"、"往左移"、"画一个房子"、"五个圆排成一排" 返回固定响应包络。
  3. `/api/parse` 支持 `model: "mock"` 或 `.env` 开关选择 MockProvider。
  4. 将 fixtures 存在 `server/src/fixtures` 或同等目录，作为后续自动化测试素材。
- **验收**：不配置任何真实 API key 时，前端仍能通过 `/api/parse` 跑通红圆最小闭环。
- **文档锚点**：大脑设计 §3、§4.2。
- **优先级**：P0。

### T2.3 后端 LLM 代理（ModelProvider + Claude）✅

- **目标**：前端只调 `/api/parse`，后端注入 key 调 Claude 返回严格 JSON。
- **步骤**：
  1. 实现 `ClaudeProvider`（Anthropic 官方 SDK/API），复用 T2.2 的 `ModelProvider` 接口。
  2. 把 system prompt + 紧凑场景图 + transcript 组装请求。
  3. `/api/parse` 接收 `{transcript, scene, groups, recentTurns, canvasSize, model}`，返回 LLM 的 JSON。
  4. **安全提示**：这是本地代理，无鉴权可接受（演示用本地裸跑）；若日后公开需加访问控制。
- **验收**：用 curl/Postman 发一条 transcript，后端返回合法的响应包络 JSON。
- **文档锚点**：MVP方案 §四；大脑设计 §0、§11。
- **优先级**：P0。

### T2.4 拆解 system prompt 落地 ✅

- **目标**：把 `大脑设计.md` §4.1 prompt 骨架 + §4.2 few-shot 写成实际 prompt 模板。
- **步骤**：
  1. 实现紧凑场景图序列化（§1.6 单行文本格式，token 降 60–70%）。
  2. 把画布信息、定位说明、当前场景图、最近 3 轮对话、核心规则、few-shot 示例 A–F 拼成 system message。
- **验收**：空画布下"画一个红色的圆"返回与示例 A 一致结构的 JSON。
- **文档锚点**：大脑设计 §4、§1.6。
- **优先级**：P0。

### T2.5 接 Web Speech API + ASR 状态机雏形（含 standby）✅

- **目标**：实现语音识别，区分 partial/final，搭起状态机骨架。
- **步骤**：
  1. 封装 Web Speech API（中文识别），首次请求麦克风授权。
  2. 实现状态机核心态：`idle / listening(active) / standby / parsing / executing / speaking`（§7.1）。
  3. **partial 只用于思考流展示，绝不触发解析**；只有 final 进入 finalizing → parsing（§7.2）。
  4. `speaking`（TTS 播报）时抑制识别防回声自触发。
  5. `standby` 态 mic/ASR 仍开，只对 "继续/开始绘图" 唤醒词做本地匹配，其余 final 忽略、不调 LLM、不画图。
- **验收**：说话时侧边栏实时显示 partial 文本；final 触发一次解析；播报时不会把自己的声音当输入；说 "暂停监听" 后进入 standby，说 "继续" 可纯语音恢复。
- **文档锚点**：大脑设计 §7。
- **优先级**：P0。

### T2.6 串联完整闭环（说话 → 解析 → 校验 → 执行）✅

- **目标**：把 T2.1–T2.5 接成一条线。
- **步骤**：final transcript → 组装 `/api/parse` 请求 → 收到 JSON → Zod 校验 → 执行器 apply → 更新场景图 → TTS 播 `reply`。
- **验收**：对着麦克风说"画一个红色的圆""把它变成蓝色""往左移""撤销"，画布正确响应。
- **文档锚点**：大脑设计 §0、§5。
- **优先级**：P0（这是 Day1 的核心里程碑）。

### T2.7 语音最小闭环 PR 检查点 ✅

- **验收**：阶段 2 的变更按单功能拆成小 PR（如 `mock provider`、`Claude provider`、`prompt template`、`ASR state machine`、`voice loop integration`），每个 PR 描述完整。阶段结束时能演示纯语音画出第一个图形。

---

## 阶段 3 — 大脑深度层（Day2 上午）

> 目标：把项目从"语音查表 demo"拉升到"有上下文记忆和复杂拆解能力的语义引擎"。这一阶段是评分重点。

### T3.1 确定性布局器：relative 四方向 ✅

- **目标**：在阶段 1 已有 `absolute/anchor` 的基础上，实现相对已有对象或同批次 `tempId` 的定位。
- **步骤**：
  1. `relative`：解析 `ref`（含同批次 `tempId → 真实 id`），按 side（left/right/above/below）放外侧 gap 处，另一轴居中（§2.3）。
  2. 支持 `dx/dy` 微调与负 gap，用于房子门窗嵌入。
  3. 兜底链：relative 引用失败 → anchor center → absolute 中心 → 边界 clamp（§2.5）。
- **验收**："在圆的左边画个方块"位置正确；房子示例中门窗能通过 `ref:"wall"` 相对墙体定位。
- **文档锚点**：大脑设计 §2.3、§2.5。
- **优先级**：P0。

### T3.2 确定性布局器：layout（row + grid）✅

- **目标**：批量布局成员，做后处理 pass。
- **步骤**：
  1. `row`：总宽 = Σ成员宽 + gap×(count−1)，整排水平居中，align 决定 y（§2.4）。
  2. `grid`：未指定列数时 `cols = ceil(sqrt(count))`，单元格取成员最大 bbox + gap。
  3. 同 `layoutId` 的 ops 收集后统一计算各自中心坐标。
- **验收**："画五个由小到大的圆排成一排""画一个 3x3 网格圆"布局整齐居中。
- **文档锚点**：大脑设计 §2.4；指令清单 L5。
- **优先级**：P0。

### T3.3 Group 对象与组合体整体操作 ✅

- **目标**：让"房子/车/太阳"成为一等公民，可整体移动/缩放/删除。
- **步骤**：
  1. 实现 `createGroup` + 带 `groupId` 的 create，建立 group 索引（成员 id + 缓存 bbox）。
  2. group 级操作传播规则（§1.3 表）：move 整组平移、scale 以 bbox 中心等比、delete 删全部成员+记录、setStyle 按语义。
  3. 成员变动即重算 bbox，撤销以整组为单位。
- **验收**："画一个房子"生成墙+屋顶+门+窗一组（示例 D），"把那个房子往左移"整组移动。
- **文档锚点**：大脑设计 §1.3、§4.2 示例 D。
- **优先级**：P0。

### T3.4 复杂指令拆解联调 ✅

- **目标**：验证 prompt + 布局器 + group 协同，覆盖 L5 核心。
- **步骤**：用演示脚本逐条测："在右边画房子"、"五个由小到大彩虹圆排成一排"、"3x3 网格圆"、"在房子左边画个圆"。调 prompt few-shot 直到稳定。
- **验收**：L5 的 P0 条目（语义组合体/数量循环/渐变排列/网格/相对定位）全部可用。
- **文档锚点**：指令清单 L5；大脑设计 §4.2。
- **优先级**：P0。

### T3.5 上下文指代与相对操作（L4）✅

- **目标**：实现"它/刚才的/最大的/第二个/红色的那个"指代消解。
- **步骤**：
  1. 主路径：紧凑场景图 + 最近 3 轮对话喂 LLM，由它出 `targetIds`（§1.7 第一层）。
  2. 兜底：代码确定性规则（上一个→lastCreatedIds、它→focusIds、最大/最小→bbox 排序、那个房子→group.label，§1.7 第二层）。
  3. 相对位移/缩放走模糊量词约定表（§3.5）。
- **验收**："把它变大""把最大的那个删掉""刚才那个圆改成绿色""往左移一点"全部命中正确对象。
- **文档锚点**：大脑设计 §1.7、§3.5；指令清单 L4。
- **优先级**：P1（深度点，时间允许必做）。

### T3.6 模型路由与故障转移（FallbackProvider）✅

- **目标**：Claude 主用，失败自动兜底 DeepSeek，前端可手动切。
- **步骤**：
  1. 实现 `DeepSeekProvider`（OpenAI-compatible HTTP）。
  2. 实现 `FallbackProvider`：默认 Claude，超时/失败/JSON 不合法时自动切 DeepSeek。
  3. 前端只发 `model` 参数，后端按参数路由（切模型不切 key）。
- **验收**：手动让 Claude key 失效，请求自动 fallback 到 DeepSeek，前端无感；"切换到 DeepSeek"语音/按钮可切。
- **文档锚点**：MVP方案 §四；大脑设计 §8.3。
- **优先级**：故障转移 P0 / 手动切换 P2。
- **可并行**：与 T3.5 可并行（不同人/不同文件）。

### T3.7 大脑深度层 PR 检查点 ✅

- **验收**：阶段 3 的变更按单功能拆成小 PR（如 `relative layout`、`row/grid layout`、`group operations`、`context reference`、`fallback provider`）。阶段结束时能演示房子拆解、渐变圆排列、上下文指代、模型自动兜底。

---

## 阶段 4 — 容错、思考流与收尾（Day2 下午）

> 目标：补齐题目硬指标"容错性"，把智能层可视化（思考流），打磨 UI，完成交付物。

### T4.1 危险指令二次确认（pendingAction，P0-lite）✅

- **目标**：清空/删除全部不会因一句口误误删整画。
- **步骤**：
  1. 执行层遇 `clear` / delete 全部对象 → 不执行，设 `pendingAction`，语音播报"确定要清空吗？说『确定』继续"。
  2. 下一句**前端本地词匹配**（不调 LLM）：命中确认词→执行；其他→取消并正常处理这句（§6.2）。
- **验收**："清空画布" → AI 反问 → 说"确定"才清空，说别的则取消。
- **文档锚点**：大脑设计 §6.2；指令清单 L6。
- **优先级**：P0-lite。

### T4.2 澄清对话协议（clarify）✅

- **目标**：模糊/歧义指令主动反问而非乱画。
- **步骤**：
  1. LLM 解析不出唯一目标 → `operations: []` + `clarify: {question, options}`（§3.1、示例 F）。
  2. 前端展示问题 + 语音播报，把澄清问答并入 recentTurns，下一句重新解析（§6.1）。
- **验收**：多对象时说"把那个东西弄红"→ AI 反问"你是指圆还是方块？"→ 回答后正确执行。
- **文档锚点**：大脑设计 §6.1、§4.2 示例 F；指令清单 L6。
- **优先级**：P1。

### T4.3 容错兜底（缺参/口误/无法理解/越界/非法）✅

- **目标**：覆盖 L6 其余条目。
- **步骤**：缺参数用默认值（P0）；口误模糊匹配（P1）；无关话礼貌提示（P1）；坐标越界 clamp（P2）；删除不存在对象友好提示（P1）；部分成功策略按 ExecReport 播报（§3.4）。
- **验收**："画个圆"无颜色用默认色；"删掉第十个"（只有3个）友好提示不存在。
- **文档锚点**：大脑设计 §3.4、§5；指令清单 L6。
- **优先级**：缺参推断 P0 / 其余 P1–P2。

### T4.4 standby 暂停监听边界测试与打磨 ✅

- **目标**：在阶段 2 已实现 standby 骨架的基础上，补齐边界测试与演示表现，确保纯语音暂停/继续不破闭环。
- **步骤**：
  1. 测试 "暂停监听/停一下"→ standby；"继续/开始绘图"→ active。
  2. standby 下除唤醒词外的所有 final transcript 只进思考流，不调 LLM、不画图。
  3. 撤销/确认词等本地快速通道与 standby/pendingAction 不互相抢状态。
- **验收**：说"暂停监听"后只响应"继续"，其余转写忽略不画图；说"继续"恢复；演示脚本中可稳定展示纯语音暂停/继续。
- **文档锚点**：大脑设计 §7、§8；指令清单 L3。
- **优先级**：P0。

### T4.5 实时思考流侧边栏 ✅

- **目标**：把大脑工作过程可视化（核心亮点）。
- **步骤**：映射管线各阶段：🎤正在听 → 📝识别到「…」 → 🧠调用{模型}拆解中 → 📐生成N个操作/算布局 → ✅已执行/❓需澄清/⚠️等待确认（§0、MVP方案 §五）。由前端 ASR 状态机阶段状态驱动（REST P0，思考流用阶段状态模拟，掩盖等待感）。
- **验收**：每说一句，侧边栏按阶段推进显示；调用模型、生成操作数、布局计算肉眼可见。
- **文档锚点**：大脑设计 §0、§8.5；MVP方案 §五。
- **优先级**：P1（最佳"亮深度"手段，强烈建议做）。

### T4.6 UI 打磨与监听状态显示 ✅

- **目标**：全屏干净界面，三档电源态可视。
- **步骤**：中央大画布；侧边栏含思考流、监听状态（🟢active/🟡standby/⚫off）、当前模型显示+切换、暂停/继续按钮（仅开发/故障恢复）、可选指令历史。
- **验收**：界面整洁、状态清晰，适合录视频。
- **文档锚点**：MVP方案 §五「界面布局」「监听电源态」。
- **优先级**：P1。

### T4.7 自测评测表（加分项）⬜

- **目标**：用数据证明严谨设计。
- **步骤**：准备简单/复杂/模糊指令各 N 条，跑一遍记录识别准确率、拆解成功率、平均延迟，整理成表。
- **验收**：`docs/` 出一份评测数据表。
- **文档锚点**：MVP方案 §六.5、§八.3。
- **优先级**：P2（有余力做，叙事价值高）。

### T4.8 交付物：文档补齐 + 视频 + 仓库 ⬜

- **目标**：完成三件交付物。
- **步骤**：
  1. **设计文档**：回填 `指令能力清单.md` 实现统计表（计划/已实现/完成率），写未完成项及原因 + 成本/延迟优化技巧 + 评测数据。
  2. **README**：在 T0.4 骨架基础上补齐最终功能说明、演示指令、常见问题。
  3. **演示视频**：按演示脚本（指令清单 §演示脚本）由浅入深拍，强调"全程未碰鼠标键盘（除首次授权麦克风）"。
  4. 最终安全检查：确认仓库 key 不泄露、`.env` 未入库、PR 已按单功能拆分。
- **验收**：仓库可被他人 clone 后按 README 跑起来；视频覆盖 L1→L6 亮点。
- **文档锚点**：MVP方案 §八；指令清单 §演示脚本、§实现情况统计。
- **优先级**：P0（交付物本身）。

---

## 附录 A — 任务与能力清单映射速查

| 设计文档能力分级 | 对应任务 |
|---|---|
| L1 基础图形 | T1.2, T1.3, T2.5 |
| L2 样式属性 | T1.3（带位置创建与 anchor） |
| L3 画布控制（撤销/清空/暂停继续/导出/切模型） | T1.4, T2.5, T4.1, T4.4, T3.6 |
| L4 上下文操作 | T3.5 |
| L5 复杂组合 | T3.1, T3.2, T3.3, T3.4 |
| L6 容错澄清 | T4.1, T4.2, T4.3 |

## 附录 B — 风险与建议

- **最大风险是阶段 2 的语音闭环**：Web Speech API 中文识别质量、回声自触发、partial/final 处理容易踩坑。建议 T2.5 预留充足调试时间，识别差时按文档降级 Whisper；同时用 T2.2 MockProvider 保证非模型链路可独立开发。
- **优先级纪律**：优先级 P0 没全绿之前不要碰优先级 P2。若 Day2 时间紧，砍 T4.7（评测表）、T3.6 手动切换、L5/L6 的 P2 条目，保住房子拆解 + 上下文指代 + 容错澄清这三个核心亮点。
- **小 PR 纪律**：不要用“每阶段一个大提交”替代 PR。阶段检查点只负责确认一组小 PR 合并后形成可演示版本；每个 PR 仍只做一件事。
- **演示路径全程语音**：开发期按钮（T1.5、T4.6）仅作调试/故障恢复，录视频时不碰。
