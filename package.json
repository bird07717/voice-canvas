{
  "name": "voice-canvas",
  "version": "1.0.0",
  "description": "Pure voice controlled drawing tool MVP.",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "build:client": "npm run build --workspace client",
    "build:server": "npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run build --workspace client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bird07717/voice-canvas.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/bird07717/voice-canvas/issues"
  },
  "homepage": "https://github.com/bird07717/voice-canvas#readme",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
